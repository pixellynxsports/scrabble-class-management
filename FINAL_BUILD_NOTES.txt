FINAL BUILD NOTES
2026-09-20

This build restores the original working login HTML and event handling from the last known working project version.

Student Profile and Edit Student improvements are retained in javascript.js and style.css.

The login page intentionally uses the original inline handlers and original initSupabaseAuth startup path. No extra login event binding script is included.

The Parent Login button has one id only.

Cache versions were updated to 20260920.4.

Resend email notification remains in submit-order-payment-proof/index.ts.

Do not change Supabase Auth settings for this frontend build.
